export type ProductsTypes = {
    data:[
        products: {
            brand: string;
            createdAt: string;
            description: string;
            id: number;
            name: string;
            photo: string;
            price: string;
            updatedAt: string;
        }
    ]

} 